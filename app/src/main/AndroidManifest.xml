<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cmdv.mymarvelapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MarvelApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMarvelApp">

        <activity
            android:name="com.cmdv.feature.splash.SplashActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.SplashActivity.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.cmdv.feature.characters.CharactersActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.CharactersActivity" />

        <activity
            android:name="com.cmdv.feature.characterdetails.CharacterDetailsActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.CharactersActivity"/>
    </application>

</manifest>